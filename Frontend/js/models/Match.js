import {updateMatch} from "./../api.js";
export default class Match{
    constructor(id, candidate, company, state){
        this.id = id;
        this.candidate = candidate;
        this.company = company;
        this.state = state;
    }
    hireCandidate(){
        //code
    }
    discardCandidate(){
        //code
    }
}
